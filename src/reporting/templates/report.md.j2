{% macro fmt(info, key, pct=False) -%}
{%- set mean_key = key ~ '_mean' -%}
{%- if info[mean_key] is defined -%}
{%- set std_key = key ~ '_std' -%}
{%- set m = info[mean_key] -%}
{%- set s = info[std_key] | default(0.0) -%}
{%- if pct -%}
{{ "%.2f"|format(m * 100) }} ± {{ "%.2f"|format(s * 100) }}%
{%- else -%}
{{ "%.4f"|format(m) }} ± {{ "%.4f"|format(s) }}
{%- endif -%}
{%- elif info[key] is defined and info[key] is number -%}
{%- if pct -%}
{{ "%.2f"|format(info[key] * 100) }}%
{%- else -%}
{{ "%.4f"|format(info[key]) }}
{%- endif -%}
{%- else -%}
-
{%- endif -%}
{%- endmacro %}

# 实验报告: {{ task_name }}

> 自动生成于 {{ timestamp }}

## 1. 实验配置

| 项目 | 值 |
|------|-----|
| 任务名称 | {{ task_name }} |
| 随机种子 | {{ seeds | join(', ') }} (n={{ seeds | length }}) |
| 设备 | {{ device }} |
| 数据集 | {{ dataset_name }} |
| 样本数 | {{ num_samples }} |
| 图像尺寸 | {{ image_size }} |
| 目标模型 | {{ model_name }} ({{ model_weights }}) |

{% if seeds | length > 1 %}
> **多种子可靠性**: 本实验使用 {{ seeds | length }} 个随机种子独立运行，指标以 均值 ± 标准差 报告。
{% endif %}

### 攻击方法

| 名称 | 参数 |
|------|------|
{% for attack in attacks -%}
| {{ attack.name }} | {% for k, v in attack.items() if k != 'name' %}{{ k }}={{ v }}{% if not loop.last %}, {% endif %}{% endfor %} |
{% endfor %}

### 防御方法

| 名称 | 参数 |
|------|------|
{% for defense in defenses -%}
| {{ defense.name }} | {% for k, v in defense.items() if k != 'name' %}{{ k }}={{ v }}{% if not loop.last %}, {% endif %}{% endfor %} |
{% endfor %}

## 2. 攻击评估

{% if structured_attacks %}
| 攻击方法 | 成功率 | 干净正确样本 ASR | 预测变化率 | 干净准确率 | 对抗准确率 | LPIPS | CLIP Score | FID |
|----------|--------|----------------|------------|-----------|-----------|-------|-----------|-----|
{% for name, info in structured_attacks.items() -%}
| {{ name }} | {{ fmt(info, 'asr', pct=True) }} | {{ fmt(info, 'asr_on_clean_correct', pct=True) }} | {{ fmt(info, 'prediction_change_rate', pct=True) }} | {{ fmt(info, 'clean_accuracy', pct=True) }} | {{ fmt(info, 'adversarial_accuracy', pct=True) }} | {{ fmt(info, 'lpips') }} | {{ fmt(info, 'clip_score') }} | {{ fmt(info, 'fid') }} |
{% endfor %}
{% else %}
{% for key, value in metrics.items() %}
{% if key.endswith('_asr') or key.endswith('_asr_mean') %}
- **{{ key }}**: {{ "%.4f"|format(value) }}
{% endif %}
{% endfor %}
{% endif %}

## 3. 防御评估

{% if structured_defenses %}
| 攻击-防御 | 鲁棒准确率 | 干净准确率下降 | 干净防御准确率 | 延迟 (s) | 实际后端 |
|-----------|-----------|---------------|---------------|----------|----------|
{% for name, info in structured_defenses.items() -%}
| {{ name }} | {{ fmt(info, 'robust_accuracy', pct=True) }} | {{ fmt(info, 'clean_accuracy_drop', pct=True) }} | {{ fmt(info, 'clean_defended_accuracy', pct=True) }} | {{ fmt(info, 'latency_mean') }} | {{ info.metadata.actual_backend if info.metadata.actual_backend is defined else info.metadata.backend if info.metadata.backend is defined else "-" }} |
{% endfor %}
{% endif %}

## 4. 完整指标

| 指标 | 值 |
|------|-----|
{% for key, value in metrics.items() -%}
| {{ key }} | {{ "%.4f"|format(value) if value is number else value }} |
{% endfor %}

{% if robustness_score is not none %}
### 综合鲁棒性评分

**{{ "%.4f"|format(robustness_score) }}** / 1.0000

> 权重: RA={{ weights.robust_accuracy }}, (1-ASR)={{ weights.inverse_asr }}, 语义质量={{ weights.semantic_quality }}, 干净保留={{ weights.clean_retention }}, 效率={{ weights.efficiency }}
{% endif %}

## 5. 资源消耗

{% if resource_metrics %}
| 指标 | 值 |
|------|-----|
{% for name, value in resource_metrics.items() -%}
| {{ name }} | {{ value }} |
{% endfor %}

{% if resource_metrics.get('gpu_mem_reserved_mb', 0) > 0 %}
> **并发估算**: 单实验 GPU 显存峰值约 {{ resource_metrics.gpu_mem_reserved_mb }} MB。在 80 GB GPU 上，安全并发数约为 {{ (80000 * 0.85 // resource_metrics.gpu_mem_reserved_mb) | int }}（留 15% 余量）。
{% endif %}
{% else %}
暂无资源消耗数据。在 GPU 环境下运行实验以收集显存与利用率指标。
{% endif %}

## 6. 可视化

{% if figures.sample_grid %}
### 样本对比

![样本对比图](figures/sample_grid.png)
{% endif %}

{% if figures.radar %}
### 鲁棒性雷达图

![雷达图](figures/radar.png)
{% endif %}

{% if figures.metric_bars %}
### 指标对比

![指标柱状图](figures/metric_bars.png)
{% endif %}

## 7. 结论

{% for line in conclusions -%}
- {{ line }}
{% endfor %}

---
*报告由 AIGC 鲁棒性平台自动生成 | {{ timestamp }}*
